# TableTech API Environment Configuration
# Copy this file to .env and fill in your actual values

# ===========================================
# 🚨 REQUIRED - Email Configuration
# ===========================================
# Resend API key (get from https://resend.com/api-keys)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxx

# From email address (must be verified domain in Resend)
# Format: "Display Name <email@domain.com>" or just "email@domain.com"
MAIL_FROM="TableTech Team <noreply@tabletech.nl>"

# Internal email where appointment notifications are sent
MAIL_TO_INTERNAL="info@tabletech.nl"

# ===========================================
# 📧 Brand & Contact Information
# ===========================================
BRAND_NAME="TableTech"
BRAND_COLOR="#E86C28"
ACCENT_COLOR="#FFB366"
SUPPORT_EMAIL="info@tabletech.nl"
WEBSITE_URL="https://tabletech.nl"
ADDRESS="Biezelingeplein 32, 3086 SB Rotterdam, Nederland"
PUBLIC_FAVICON_URL="https://tabletech.nl/favicon.ico"

# ===========================================
# 🔐 Security Configuration
# ===========================================
# Captcha settings (Cloudflare Turnstile recommended)
ENABLE_CAPTCHA="true"
CAPTCHA_PROVIDER="turnstile"
CAPTCHA_SECRET="your_turnstile_secret_key_here"

# Rate limiting (max requests per window)
RATE_LIMIT_MAX="5"
RATE_LIMIT_WINDOW="600000"

# Anti-spam settings
ENABLE_DISPOSABLE_CHECK="true"
MIN_FORM_FILL_TIME="1200"
MAX_URLS_IN_MESSAGE="3"
MAX_MESSAGE_LENGTH="5000"

# ===========================================
# 🌐 CORS & Environment
# ===========================================
# Allowed origins (comma-separated)
ALLOWED_ORIGINS="https://tabletech.nl,http://localhost:5173"

# Node environment
NODE_ENV="production"
PORT="3001"

# Logging level
LOG_LEVEL="info"

# ===========================================
# 🗄️ Optional - Redis (for distributed rate limiting)
# ===========================================
# REDIS_URL="redis://localhost:6379"

# ===========================================
# 📝 Setup Instructions:
# ===========================================
# 1. Sign up for Resend account at https://resend.com
# 2. Add your domain (tabletech.nl) to Resend
# 3. Verify domain ownership via DNS records:
#    - Add SPF record: v=spf1 include:_spf.resend.com ~all
#    - Add DKIM record: Copy from Resend dashboard
#    - Add DMARC record: v=DMARC1; p=quarantine; rua=mailto:dmarc@tabletech.nl
# 4. Create API key and add to RESEND_API_KEY
# 5. Get Cloudflare Turnstile keys at https://dash.cloudflare.com/
# 6. Update CAPTCHA_SECRET with your secret key
# 7. Test with development settings first
# 8. Important: Use professional "from" address like noreply@tabletech.nl
# 9. Avoid spam words in subject lines (FREE, WIN, URGENT, !!!, etc.)